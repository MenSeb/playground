<svg class="icons" aria-hidden="true">
    <defs>
        {{#each icons as |icon| }}
        <symbol id="icon-{{ icon.name }}" viewBox="0 0 24 24">
            <path d="{{ icon.path }}" />
        </symbol>
        {{/each}}
    </defs>
</svg>
